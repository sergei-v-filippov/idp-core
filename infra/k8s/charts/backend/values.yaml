# Backend Configuration

image:
  repository: sergeivf/idp-core-backend
  tag: "latest" # This will be overwritten by CI
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

databaseSecretName: idp-backend-db-app

# Environment variables for the application
# env:
#   DATABASE_URL: "postgres://user:pass@localhost:5432/db" # Placeholder