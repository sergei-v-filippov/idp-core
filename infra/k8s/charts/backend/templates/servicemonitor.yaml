apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-monitor
  labels:
    # This label is required for Prometheus Operator to discover this monitor
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: backend # Matches the label in service.yaml
  endpoints:
  - port: http # Refers to the port name in service.yaml
    path: /metrics
    interval: 15s
  namespaceSelector:
    matchNames:
    - default